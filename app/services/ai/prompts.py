"""Промпты для AI-анализа обращений.

Вынесены в отдельный модуль, чтобы их было легко ревьюить, версионировать и
переиспользовать между провайдерами. Полное описание — в docs/ai-integration.md.

Задача одним вызовом: тональность + категория + приоритет + черновик ответа.
Объединение трёх функций в один вызов экономит latency и токены (см. ADR-0003).
"""

from __future__ import annotations

SYSTEM_PROMPT = """\
Ты — ассистент разработчика, который обрабатывает обращения с формы обратной \
связи на его сайте-портфолio. Твоя задача — проанализировать сообщение и вернуть \
строгий структурированный результат.

Правила:
- sentiment: тональность сообщения (positive | neutral | negative).
- category: тип обращения:
    job_offer — предложение о работе, вакансия, оффер;
    project   — заказ проекта, подряд, сотрудничество;
    question  — вопрос по опыту, стеку, технологиям;
    spam      — реклама, нерелевантный массовый спам;
    other     — всё остальное.
- priority: приоритет ответа (high | medium | low). Офферы и проекты обычно \
high, вопросы — medium, спам — low.
- suggested_reply: вежливый черновик ответа на русском языке от первого лица, \
2–4 предложения, по тону соответствующий обращению. Без выдуманных фактов и \
конкретных дат/цен.

Отвечай только через предоставленный инструмент."""

USER_PROMPT_TEMPLATE = """\
Имя отправителя: {name}

Текст обращения:
\"\"\"
{comment}
\"\"\"

Проанализируй обращение и верни результат."""


# Схема инструмента для structured output (Anthropic tool-use).
# Гарантирует, что модель вернёт валидный JSON нужной формы.
ANALYZE_TOOL = {
    "name": "submit_analysis",
    "description": "Вернуть структурированный анализ обращения",
    "input_schema": {
        "type": "object",
        "properties": {
            "sentiment": {
                "type": "string",
                "enum": ["positive", "neutral", "negative"],
            },
            "category": {
                "type": "string",
                "enum": ["job_offer", "project", "question", "spam", "other"],
            },
            "priority": {
                "type": "string",
                "enum": ["high", "medium", "low"],
            },
            "suggested_reply": {"type": "string"},
        },
        "required": ["sentiment", "category", "priority", "suggested_reply"],
    },
}
